Task 25: [NEW] Enhanced Blind Feature with Scroll-Wheel Opacity & Rich Shutter Animation

## Objective
Enhance the blind shutter overlay with rich glassmorphism textures, metallic slat shadow gradients, smooth top-to-bottom drop animations, and mouse-wheel opacity/density adjustment (30% to 100%).

## DoD (Definition of Done)
1. In `style.css`, upgrade `.blind-shutter` and `.blind-content` styling:
   - Add rich metallic slat gradients, subtle drop-shadows, and glassmorphism backdrop blur.
   - Smooth top-to-bottom sliding transition using `transform: translateY` with a custom `cubic-bezier(0.16, 1, 0.3, 1)`.
2. In `js/app.js`, add a `wheel` event listener to active `.blind-shutter` elements:
   - Scrolling up increases opacity/density (up to 1.0 / 100%).
   - Scrolling down decreases opacity/density (down to 0.3 / 30%).
   - Show a subtle percentage badge indicator (`30%` to `100%`) when scrolling.
3. Ensure all 14 existing unit tests in `tests/store.test.js` continue to pass cleanly (100% green).

## Allowed Scope
- index.html
- style.css
- js/app.js

## Protected Paths (DO NOT MODIFY)
- .nightly/
- .github/
- AGENTS.md
- task_template.md
